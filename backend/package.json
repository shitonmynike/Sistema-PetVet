{
  "name": "petvet-backend",
  "version": "1.0.0",
  "description": "Sistema de agendamento para clínica veterinária PetVet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --env-file=.env server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["veterinaria", "agendamento", "petvet", "nodejs", "express"],
  "author": "PetVet Team",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongodb": "^6.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
